# Event Log — 2026-09

> 事件流水帳，由 git hooks 和 AI Agent 共同維護。
> **嚴禁修改已有條目**，只能 append。
> 詳見 `log/README.md`。

## [2026-09-07 09:10] feature | 送照樓板面積計算式 + 停車格編號 + 圖面順序編號（3 個新工具）
- actor: claude-opus-5 (via Claude Code)
- files: MCP-Server/src/tools/area-formula-tools.ts, MCP-Server/src/tools/numbering-tools.ts, MCP-Server/src/tools/index.ts, MCP/Core/CommandExecutor.cs, MCP/Core/Commands/CommandExecutor.AreaFormula.cs, MCP/Core/Commands/CommandExecutor.Numbering.cs, domain/permit-floor-area-formula.md, domain/sequence-numbering.md, domain/parking-auto-numbering.md, tests/RevitMCP.Tests.AreaFormulaNumbering/, CLAUDE.md, README.md, README.zh-TW.md, docs/
- trigger: manual
- summary: 新增 create_floor_area_formula / renumber_parking_spaces / create_sequence_numbers（tools 180→183、domain 79→81）。parking-auto-numbering.md v1.0 指向的 scripts/number_parking.js 從未進過版控，本次把方法落地成真工具並在文件記錄該死指標。新增離線 harness 53 checks，6 個 mutation 探針全數轉紅。QA/QC -SkipBuild -SkipDeploy 通過。DLL 尚未部署（Revit 執行中鎖檔），實機驗證待辦。

## [2026-09-17 09:30] feat | align_structural_framing 從 wip/align-framing 接回，修方向 bug，Tools 180→181

- actor: claude-opus-5 (via Claude Code)
- files: MCP/Core/Commands/CommandExecutor.FramingAlign.cs, MCP/Core/CommandExecutor.cs, MCP-Server/src/tools/structure-tools.ts, tests/RevitMCP.Tests.FramingAlign/, pyRevit_Tools/.../AlignFraming.pushbutton/script.py, docs/BIM_MCP/reference/tools-index.html, 26 處計數宣稱
- trigger: 使用者指定續做 AlignFraming（2026-07-23 停工至今）
- summary: `wip/align-framing`（5ef7ee2，合併前快照）上的 C# 移植與純幾何測試接回以 main 為基底的 `feat/align-framing`。接回時發現兩側共有的方向 bug：橫向 containment 直接比世界 Y，三組校準樑都沿 X 所以沒暴露；沿 Y 的樑會讓 X 向偏 2 m 的柱通過再被柱豁免放行。改用 perp 投影，C# 與 pyRevit 同修。
- verification: 幾何測試 11/11 GREEN（新增 2 條 Y 向樑案例，修前 RED 1）；pyRevit 側 headless stub 探針同樣 RED→GREEN；`dotnet build Release.R23` 0 error；`npm run build` 通過；`verify-qaqc.ps1` 70 PASS / 0 FAIL / 1 WARN（既有隔離項）。**尚未在真實 Revit 模型驗證**——地真值模型（樑 2063997 / 2067719 / 2065055）不是目前開啟的檔，三案例回歸待部署後執行。

## [2026-09-17 11:40] fix | area-formula / numbering 三工具依審查修 2 Critical + 3 Important，合回 main 前的最後一輪

- actor: claude-opus-5 (via Claude Code)
- files: MCP/Core/Commands/CommandExecutor.Numbering.cs, MCP/Core/Commands/CommandExecutor.AreaFormula.cs, MCP-Server/src/tools/numbering-tools.ts, MCP-Server/src/tools/area-formula-tools.ts, tests/RevitMCP.Tests.AreaFormulaNumbering/Program.cs
- trigger: code-reviewer 對 feat/area-formula-numbering（9/07 未 commit 的 WIP，從沒在 Revit 跑過）回 REQUEST CHANGES；使用者選「Critical + 機械式 Important 修完合併，設計類三項留 follow-up」
- summary: (1) `ReadStringArray` 遇到非陣列 token 靜默退回預設 → 改丟例外，新增 `ReadIdArray` 給 `sourceIds`/`elementIds`，同樣不得靜默退回自動收集；(2) 三處 `trans.Commit()` 回傳值被忽略 → `!= Committed` 就丟例外（SilentFailuresPreprocessor 對 Error 級失敗回 Continue，Commit 會 RolledBack 但不丟）；(3) `create_sequence_numbers` / `create_floor_area_formula` 的 `dryRun` 預設 false → true，與 `renumber_parking_spaces` 及 domain 文件一致；(4) 停車格參數未指定時改 `BuiltInParameter.ALL_MODEL_INSTANCE_COMMENTS`，不再靠 zh-TW 顯示名 `LookupParameter("備註")`，且參數不存在／唯讀／非文字在 dry-run 就算進 `Spaces[].ParamIssue` 與 `ParamIssueCount`；(5) TextNote 換行改單一 `\r`（抽成純函式 `BuildTextNoteText`）。**未修、留 follow-up**：AreaFormula 只讀 `loops[0]`（內環區域公式高估但對帳仍綠，應改對 `spatial.Area`）、對帳不符仍寫 TextNote、取不到中心點的元素靜默 `continue`。
- verification: harness 65→79 checks（新增 14 條，修前 RED：2 FAIL + `ReadIdArray` method not found）；`dotnet build Release.R23` 0 error；`npm run build` 通過；FramingAlign 11/11；verify-qaqc -SkipBuild -SkipDeploy FAIL 0 / WARN 1。**仍未在 Revit 實機跑過**，審查列的 7 項實機風險（TextNote Z、`\r` 渲染、AreaPlan 收集、1 mm 化簡容差、面/樓板主體停車格 LevelId、群組內 Parameter.Set、schema 重啟）待 deploy 後在一張真 AreaPlan 驗。

## [2026-09-18 09:20] verify | create_floor_area_formula 真模型實測，退掉 6/7 項實機風險

- actor: claude-sonnet-5 (via Claude Code)
- files: 無程式碼變更，僅實機驗證
- trigger: 使用者要求真模型驗證 2026-09-17 review 列的 7 項實機風險
- summary: DLL 部署後 8964 無 listener，根因是 Revit 開專案不會自動啟 MCP WebSocket 服務，需在 Revit ribbon「MCP Tools」面板手動按「MCP 服務（開/關）」（見 skills/revit-mcp.md「沒 listener 時的處置」段）。啟動後在 `F:/15  雙子星/03-模型/N-模型/ARCH/TKG_地下部-ARCH.rvt` 的 D1_B1FL（121 個真房間）跑 `create_floor_area_formula`：dry-run 43 項對帳全 WithinTolerance、78 項 Undecomposed 理由對得上真幾何（弧線/非直角/多頂點）、0 Mismatch；dryRun=false 真寫入 TextNote 成功（Written=true，驗完刪除清乾淨）。
- verification: 退掉 TextNote 落地、Commit 狀態檢查、1mm 化簡容差（真牆心邊界最大 delta 0.023 ㎡）、Node schema 重啟生效、Room 邊界收集路徑（GetBoundarySegments）共 6 項。**未退**：`\r` 在 TextNote 內的實際渲染（send_code_to_revit 另一支 MCP 未連線，讀不到 TextNote.Text 原始字串肉眼確認）、Area（非 Room）收集路徑（此模型 OST_Areas=0）、停車格工具 renumber_parking_spaces（此模型 OST_Parking=0，未找模型測）。使用者選擇這裡收工，不追停車格模型。

## [2026-09-18 10:05] fix | create_floor_area_formula follow-up 三項：內環偵測、對帳不符擋寫、中心點靜默改回報（撤回 spatial.Area 半條）

- actor: claude-sonnet-5 (via Claude Code)
- files: MCP/Core/Commands/CommandExecutor.AreaFormula.cs, MCP-Server/src/tools/area-formula-tools.ts, tests/RevitMCP.Tests.AreaFormulaNumbering/Program.cs
- trigger: 使用者要求補上 2026-09-17 review 留的三項設計級 follow-up
- summary: (1) `GetBoundarySegments` 回傳的 `loops[0]` 不保證是外環，帶內環（樓梯間/管道間鏤空）的空間元素會被漏算內環、面積高估卻對帳仍過。新增純函式 `SelectOuterLoopIndex`（面積最大者為外環），`TryGetOuterLoopPoints` 回報內環數；有內環一律列入 Undecomposed，不產生算式。(2) `Mismatches` 非空時 `dryRun=false` 現在丟例外擋寫，新增 `allowMismatches`（預設 false）供確認後強制寫入。(3) 自動收集模式下中心點解不出來的元素，以前靜默 `continue` 從 `spatials` 消失，改列入 `Undecomposed`（原因「取不到中心點」）。**執行中撤回 review 建議的另一半**：原打算把對帳基準從 `PolygonAreaM2(simplified)` 改成 `spatial.Area`，但用剛驗證過的真模型資料反查——房間 3221918 的 `Room.Area`=1.67㎡ 對上我們牆心邊界的 2.03㎡，差 0.36㎡，因為 `Room.Area` 由專案「面積及體積計算」設定決定（通常算到牆面/牆心層），跟這裡 `boundaryLocation`（送照慣例算到牆心）本來就是兩種慣例、系統性不同。改下去會讓全部房間變假 Mismatch，是倒退不是修 bug，故保留原對帳基準，只修外環選取。
- verification: harness 65→79（2026-09-17）→82（新增 3 條 `SelectOuterLoopIndex` 案例，修前 RED：method not found）；`dotnet build Release.R23` 0 error；FramingAlign 11/11 無回歸；`npm run build` 通過；verify-qaqc FAIL 0 / WARN 1。**尚未在真模型重跑**：DLL 未部署（Revit 開著鎖檔），這批改動對「單環無鏤空」房間的行為應該不變（沿用同一對帳基準），但沒有真模型資料驗證 SelectOuterLoopIndex 在真的帶內環房間上的行為——D1_B1FL 121 個房間沒有已知的內環案例可測。
